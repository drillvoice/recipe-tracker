# This workflow has been intentionally disabled. It previously deployed preview builds
# to Firebase Hosting for pull requests, but the automation is no longer required.
# Keeping the file (with a manual trigger only) documents the previous behaviour while
# ensuring it will not run automatically.

name: Disabled - Deploy to Firebase Hosting on PR

on:
  workflow_dispatch:
    inputs:
      note:
        description: Optional note explaining why this workflow was triggered manually.
        required: false

permissions: {}

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled and performs no actions."
